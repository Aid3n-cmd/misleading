#NOTE: the password is not the same as the actual password in Misleading API it is just a template

#for just one password no indication for incorrect password
password = input('Enter the API password')
if password == 'correct':
  input('That password is correct')
  
  
 #for just one password ans with incorrect password indication
 password = input('Enter the API password')
 if password == 'correct':
    print('That password is correct')
 else:
    input('that password is incorrect')  
